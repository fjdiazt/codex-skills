interface:
  display_name: "FD Git Init"
  short_description: "Safely initialize project Git repos"
  default_prompt: "Use $fd-git-init to safely initialize Git for this project folder, detect nested repo boundaries, add a project-type-aware .gitignore, optionally generate a README from real project files, and configure origin, commit, or push only with explicit authorization."
