interface:
  display_name: "Question"
  short_description: "Question-only, no-change mode"
  default_prompt: "Use $fd:question: this is just a question, don't implement or change anything. Inspect read-only only if useful."
